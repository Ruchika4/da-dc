# Keep this tool's install self-contained: always use public npm and don't
# inherit a host repo's Artifactory/auth/engine-strict settings from a parent
# .npmrc. Lets the workflow's `npm ci` run anywhere without extra credentials.
registry=https://registry.npmjs.org/
always-auth=false
engine-strict=false
